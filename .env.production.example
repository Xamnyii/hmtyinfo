MONGODB_URI=mongodb://hmtyinfo_app:REPLACE_WITH_A_STRONG_PASSWORD@127.0.0.1:27017/hmtyauth?authSource=hmtyauth
MONGODB_DB=hmtyauth

# Required only when the MCP routes are used.
# MCP_SERVER_URL=http://127.0.0.1:PORT/mcp

# Required only when the CFDI/estate analyzer routes are used. Point it at the
# RamRod analyzer service (services/cfdi-analyzer).
# CFDI_ANALYZER_URL=http://127.0.0.1:8001